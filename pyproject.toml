[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "mixmasterapi"
authors = [{name = "Robert Hernes Arnorsson", email = "support@mixmaster.app"}]
dynamic = ["version", "description"]
